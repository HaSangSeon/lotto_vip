# 프로젝트 영구 규칙 (Project Permanent Guidelines)

## 1. 테스트 환경
* 사용자는 **실물 안드로이드 기기 (갤럭시 S24 Ultra)** 와 **안드로이드 에뮬레이터**로만 테스트를 수행합니다.

## 2. 모든 안드로이드 기기 고려 개발 원칙 (절대 필수)
* 모든 UI/UX 및 기능 작업 시, 특정 기기나 에뮬레이터 해상도에 국한되지 않고 **안드로이드의 모든 제조사(삼성 갤럭시, 구글 픽셀 등), 화면 비율(소형폰, 폴더블, 대형폰), 시스템 폰트 배율(Font Scale), 디스플레이 배율(DPI)**을 100% 고려하여 개발해야 합니다.
* **레이아웃 절대 원칙**:
  - `Row` 또는 `Column` 내부에서 고정 가로 너비나 패딩으로 인해 `RenderFlex overflow`가 단 1픽셀도 발생하지 않도록 항상 `Flexible`, `Expanded`, `FittedBox`, 또는 반응형 레이아웃을 기본 적용합니다.
  - 시스템 글자 크기를 '크게' 설정하거나 삼성 One UI 가변 폰트 환경에서도 텍스트가 잘리거나 넘치지 않도록 `maxLines`, `overflow: TextOverflow.ellipsis`, 유연한 마진/패딩을 철저히 준수합니다.

## 3. 임의 롤백 금지
* 사용자의 명시적인 사전 허락 없이는 이전 버전으로의 임의 원복 작업을 절대로 수행하지 않습니다.
